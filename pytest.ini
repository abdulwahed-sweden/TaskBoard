
[pytest]
DJANGO_SETTINGS_MODULE = test_settings
addopts=-v -ra
filterwarnings =
    # WhiteNoise warns that STATIC_ROOT is empty in dev/tests (no collectstatic);
    # harmless here — static serving is exercised in prod, not the test suite.
    ignore:No directory at:UserWarning
